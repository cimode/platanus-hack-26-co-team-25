{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    // src/lib/domain/matching/* import each other with explicit .ts
    // extensions so the demo runs directly under Node's type stripping
    // (`node src/lib/domain/matching/demo.ts`).
    // Safe only because noEmit is on -- tsc never has to rewrite them.
    "allowImportingTsExtensions": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  // timeline/ is a separate package (own package.json, own deps -- notably
  // zod ^3 against this project's ^4) run standalone via
  // `node --experimental-strip-types`. It resolves against timeline/node_modules,
  // not ours, so typechecking it from here can only ever produce false errors.
  "exclude": ["node_modules", "timeline"]
}
